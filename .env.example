
APP_NAME=Laravel
APP_ENV=${LARAVEL_APP_ENV}
APP_KEY=base64:Bc4jUoKW1SRwGJraWsj9Rl3Bi2Qf0rt4xqroiHKWFoM=
APP_DEBUG=${LARAVEL_APP_DEBUG}
APP_URL=${LARAVEL_APP_URL}

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=${LARAVEL_APP_DB_DRIVER}
DB_HOST=${LARAVEL_APP_DB_HOST}
DB_PORT=${LARAVEL_APP_DB_PORT}
DB_DATABASE=${LARAVEL_APP_DB_DATABASE}
DB_USERNAME=${LARAVEL_APP_DB_USERNAME}
DB_PASSWORD=${LARAVEL_APP_DB_PASSWORD}


BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=${LARAVEL_APP_AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${LARAVEL_APP_AWS_SECRET_ACCESS_KEY}
AWS_DEFAULT_REGION=${LARAVEL_APP_AWS_DEFAULT_REGION}
AWS_BUCKET=${LARAVEL_APP_AWS_BUCKET}
AWS_ENDPOINT=${LARAVEL_APP_AWS_ENDPOINT}
AWS_USE_PATH_STYLE_ENDPOINT=${LARAVEL_APP_AWS_USE_PATH_STYLE_ENDPOINT}

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

ASSET_URL="${LARAVEL_APP_ASSET_URL}"
SSO_URL="${LARAVEL_APP_SSO_URL}"
JWT_PASSPHRASE=new-sakip-ganti-jadi-sakinah-periode-2024-2031
IKD_CLIENT_ID=xx
IKD_CLIENT_SECRET=xx
IKD_URL="https://ikd-bpkad.sumbarprov.go.id"
SIMONEV_URL="https://sakatoplan-apps.sumbarprov.go.id/emonev"
